import { CouponDurationPipe } from './coupon-duration.pipe';

describe('CouponDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new CouponDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
